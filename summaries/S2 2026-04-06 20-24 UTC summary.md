# S2 2026-04-06 20:08 UTC summary

## Scope of this summary

This note captures the current S2 state after the live runtime unlock/debugging work completed on 2026-04-06.

It focuses on:

- the current end-to-end live execution state
- what was fixed in the last hours
- what is now proven to work
- what still appears incomplete or inconsistent
- the current live-validation rollout plan
- what has been learned so far about DCA and how S2 currently implements it

This summary intentionally excludes heartbeat discussion.

---

## Current high-level position

S2 has now moved beyond signal observability only and has demonstrated a real live Bybit staged-entry submission path for an enabled multi-token bot.

The clearest proof is Bot4 / `CRVUSDT`, which now successfully progressed through:

- signal receipt
- webhook auth
- parsing
- symbol routing
- actionable risk decision
- live sizing
- Bybit submission
- staged-entry / DCA add submission

This is the strongest live S2 execution proof so far.

At the same time, S2 is still in a controlled validation phase rather than full rollout. The current operating plan is to monitor live trading on the active 3-bot validation set for some days, confirm stable behaviour across real entries and trade-management lifecycle events, and only then expand the active set toward all 8 bots.

---

## Confirmed real live execution proof

### Bot4 / CRVUSDT

A replayed `ENTER_LONG_Bot4` signal reached the corrected runtime and produced:

- initial order attempt: submitted
- staged/DCA add order attempt: submitted

The resulting `order_attempts` rows showed:

- `bot_id = Bot4`
- `symbol = CRVUSDT`
- `status = submitted`
- sane notional sizes around 50 USD per staged leg

This demonstrates that S2 can now submit live staged entries into the correct Bybit bot subaccount with correct symbol routing and sensible sizing.

---

## What was fixed in the last hours

### 1) Runtime execution unlock

The runtime gate for multi-token live entry was unlocked by aligning the risk/execution path with the effective resolved bot settings rather than letting old shared safe-mode assumptions block valid entries.

This removed blockers such as:

- trading disabled
- BTC-only symbol allowlist
- low max leverage assumptions preventing active bot symbols from becoming actionable

### 2) Missing price-tick execution blocker

Execution was previously failing at sizing with:

- `No stored price tick available for sizing`

This was fixed by adding a fallback to a live Bybit market ticker price when no stored internal symbol tick exists.

This removed a fragile dependency on pre-populated tick storage.

### 3) Live sizing basis fix

A major sizing bug was identified and fixed.

The intended sizing rule is:

- `effective account balance = min(actual account balance, 5000 USD)`
- `trade notional = effective account balance × % of account to use × leverage`

The bug was that S2 had been treating `5000` as the active sizing base instead of as a cap.

This produced absurd order quantities and caused Bybit insufficient-balance rejections.

After the fix, live sizing correctly used the real subaccount balance capped at 5000.

### 4) Webhook/dashboard DB source-of-truth alignment

The dashboard and webhook runtime were aligned onto the same explicit DB path so recent runtime activity can be represented from one source of truth.

This was necessary because mobile/dashboard observability was lagging behind the live webhook/order behavior.

### 5) Mobile operator page improvements

The mobile page now correctly reflects:

- enabled bot set
- symbol mapping
- balances
- current trade state

Additional recent monitoring blocks were added for:

- last trade signal
- last order attempt
- last failed execution reason

However, one small remaining inconsistency still appears to exist in the mobile summary block’s DB-read path, described below.

---

## Current active validation set

The active S2 validation set is currently:

- Bot1 → `IPUSDT`
- Bot2 → `NEARUSDT`
- Bot4 → `CRVUSDT`

The repo config was updated to better reflect this current reality.

This is the deliberate controlled live-validation set. The current plan is:

1. continue monitoring real live trading on Bot1, Bot2, and Bot4 for some days
2. confirm stable behaviour across:
   - entries
   - DCA adds where applicable
   - TP/SL management
   - break-even behaviour
   - exit coherence
   - mobile/dashboard observability consistency
3. only after that broader confidence review, expand activation toward the full 8-bot set

So the near-term posture is:

## monitor 3 bots first, then expand to all 8

not immediate all-bot activation.

---

## Signal reality over the last 24 hours

### Important note

The currently aligned DB view shows a smaller set of fully captured/normalized recent S2 signals than Ian reports seeing from the broader live alert stream.

At the same time, Ian provided a TradingView alerts CSV showing that in the recent ~24h window there were multiple real S2 bot alerts successfully delivered across the active validation bots.

This means the following distinction matters:

### A) Strict current aligned DB truth

In the currently aligned runtime DB view, the last-24h confirmed fully normalized and automated signal path is still narrow.

### B) Broader operational / TradingView truth

From the TradingView alert log, the recent successful delivered S2 bot alerts included:

- Bot2 / NEAR:
  - `EXIT_LONG_Bot2`
  - `ENTER_SHORT_Bot2`
  - `EXIT_SHORT_Bot2`
  - `ENTER_LONG_Bot2`
- Bot1 / IP:
  - `EXIT_SHORT_Bot1`
- Bot4 / CRV:
  - `EXIT_SHORT_Bot4`
  - `ENTER_LONG_Bot4`

That is 7 successfully delivered real S2 bot alerts in the recent review window.

At the same time, only Bot4 `ENTER_LONG` has been clearly proven through the currently fixed runtime into real Bybit order submission.

So the operational reality should not be reduced to "only one signal existed" just because the normalized DB table currently shows a small recent subset.

The correct reading is:

- TradingView delivered more real bot alerts than the aligned DB summary alone suggests
- but only Bot4 currently has clear end-to-end live order-submission proof in the corrected runtime path

This distinction should be preserved in later reviews.

---

## DCA monitoring and learning so far

Recent work reviewed DCA versus no-DCA using replay/analysis based on:

- received S2 signals
- bot/symbol mapping
- public Bybit candle data
- the implemented impulse-aware staged-entry logic already present in S2

This was important because DCA had originally been treated more like a general strategy assumption, but the evidence now suggests the results are bot-dependent.

### Current replay conclusion

The current high-level conclusion remains:

- DCA is **not clearly superior globally**
- selective bot DCA is the stronger direction
- Bot2 / `NEARUSDT` has the clearest evidence in favor of keeping DCA
- Bot1 / `IPUSDT` remains mixed / inconclusive
- Bot4 / `CRVUSDT` currently looks more mixed and may lean against a default DCA assumption

The implemented replay recap at the time of the latest run was approximately:

- total reviewed entry signals: 13
- DCA better: 5
- non-DCA better: 5
- neutral: 3

By bot:

- Bot2 / `NEARUSDT`: DCA 3, non-DCA 1, neutral 0
- Bot1 / `IPUSDT`: DCA 1, non-DCA 1, neutral 2
- Bot4 / `CRVUSDT`: DCA 1, non-DCA 3, neutral 1

### Strategic learning

The practical strategic takeaway is:

## selective DCA by bot, not one-size-fits-all DCA

This is one of the more important design learnings from the recent review work.

---

## General overview of the current hybrid DCA strategy

S2’s current DCA / staged-entry approach is best described as a hybrid staged-entry model rather than a blind always-average-down system.

At a high level it works like this:

- an initial portion of the intended position is entered immediately
- a second staged add can be entered later rather than all size being deployed at once
- the later add is governed by runtime logic rather than being unconditional
- if break-even protection is already armed, the delayed add is blocked
- the runtime uses bot-resolved settings, so DCA behaviour can be controlled per bot rather than globally in one blunt way

That makes the current model “hybrid” in the sense that it combines:

- immediate market participation
- staged follow-up sizing
- runtime gating and safety checks
- break-even interaction
- the ability to evolve toward selective per-bot activation

So this is not a martingale-style uncontrolled averaging system. It is a controlled staged-entry framework that already supports more selective use.

---

## Current plan for selective bot DCA activation

The plan is not to rush a blanket DCA change across all bots.

Instead, the current expected direction is:

- keep the selective-bot-DCA decision path open as the next refinement after broader live confidence is established
- continue monitoring the active 3-bot live set first
- use that live evidence together with the replay results to decide where DCA should remain active, be reduced, or be disabled

Current leaning:

- Bot2 / `NEARUSDT`: strongest case to keep/activate DCA
- Bot1 / `IPUSDT`: monitor further before making a stronger commitment
- Bot4 / `CRVUSDT`: likely candidate for a reduced-DCA or non-DCA baseline

So the sequence is:

1. stabilize and observe controlled live 3-bot trading
2. confirm lifecycle behaviour and operator observability
3. then operationalize selective bot DCA with bot-specific policy rather than broad universal activation

---

## What is now proven

At this point, S2 has now proven all of the following in a live runtime context:

- signal receipt works
- webhook auth works
- parsing works
- multi-token symbol routing works
- actionability gating can open for enabled bots
- sizing can use real balance with a 5000 USD cap
- Bybit submission can succeed for staged entry on Bot4 / `CRVUSDT`
- the mini mobile page is now useful as a real operator view

This is a meaningful milestone improvement over the earlier state where signals were visible but no reliable live execution path existed.

---

## What still appears incomplete / still needs attention

### 1) Broader alert-stream observability

Ian is seeing more alert activity externally than is represented in the currently aligned normalized DB summary.

So there is likely still remaining work needed to ensure the DB/dashboard view captures the full operational signal stream consistently.

### 2) Mobile summary-block consistency

Although the mobile page is much improved and clearly useful now, one remaining smaller issue appears to exist in the new activity/heartbeat summary block logic, where some values have lagged or fallen back inconsistently relative to the latest runtime truth.

This does not invalidate the larger execution unlock work, but it does mean the summary block still needs a small follow-up fix.

### 3) Broader 3-bot validation still ongoing

Even though Bot4 has now proven a real live staged-entry path, broader 3-bot validation is still in progress.

It is still too early to claim the whole active set is equally proven under real live conditions.

### 4) Config debt / placeholder noise still exists

Some leftover config-validation noise still exists in `config/settings.json`, including placeholder-era values such as a `stopLoss.triggerPercent = 0.0` style setting that can still generate confusing validation messaging.

This no longer appears to block the Bot4 live execution path, but it should still be cleaned up.

---

## Practical current interpretation

The best practical reading right now is:

- S2 is no longer just a theoretical or partially wired system
- it now has a real live multi-token staged-entry path proven on Bot4 / `CRVUSDT`
- major execution blockers have been removed
- the current operational plan is to keep monitoring live trading across the controlled 3-bot set for some days before expanding toward all 8 bots
- DCA review work has shown that selective bot DCA is a better strategic direction than universal DCA
- remaining issues are now narrower and more operational/observability-focused rather than foundational

That is a strong step forward.

---

## Recommended current framing

A good concise way to describe S2 now is:

> S2 has successfully crossed from signal-only validation into real live multi-token staged-entry execution on Bybit, with Bot4 / CRVUSDT providing the clearest live proof. The major runtime blockers around actionability, tick availability, and sizing have been fixed. The current plan is to monitor controlled live trading on Bot1, Bot2, and Bot4 for some days before expanding toward all 8 bots. DCA review work now points toward selective bot DCA rather than universal DCA, with NEAR currently the strongest keep-DCA case.

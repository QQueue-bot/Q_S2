# Unified Web Portal Proposal — S2 / S4 / S6

**Date:** 2026-04-24  
**Scope:** Read-only proposal. No code changes in this document.

---

## 1. Current State Audit

### 1.1 Hosting and routing

The EC2 instance runs a single Cloudflare Tunnel (`q-s2-tunnel`) configured at `~/.cloudflared/config.yml`:

```
ingress:
  - hostname: hooks.tbotsys.one   → http://127.0.0.1:3001  (S2 webhook)
  - hostname: dashboard.tbotsys.one → http://127.0.0.1:3010  (S2 dashboard)
  - service: http_status:404
```

All other services are accessible only by direct IP:port or internally:

| Port | Service | Process | Source |
|------|---------|---------|--------|
| 3001 | S2 webhook | Node.js | `/home/ubuntu/.openclaw/workspace/Q_S2/scripts/run-webhook.js` |
| 3010 | S2 dashboard | Node.js | `/home/ubuntu/.openclaw/workspace/Q_S2/scripts/run-dashboard.js` |
| 80   | S4 classic dashboard | `python -m http.server` | `/home/ubuntu/s4/` |
| 8080 | S4_2 dashboard | `python -m http.server` | `/home/ubuntu/s4_2/` |
| 8082 | S6 Signal Scout dashboard | Flask | `/home/ubuntu/S6/main.py` |
| 8083 | S6 Funnel dashboard | Flask | `/home/ubuntu/S6_FUNNEL/main.py` |
| 8084 | S6 Toggle API | Flask | `/home/ubuntu/s6/agent/toggle_api.py` |

S4 EMA Live (`/home/ubuntu/s4_ema_live`) has no web dashboard — state lives in `state.json` and `trades.json`.

### 1.2 S2 Dashboard (dashboard.tbotsys.one / port 3010)

**How it's served:** Node.js `http.createServer` in `createDashboardServer.js`. Two routes:
- `/` — full desktop dashboard (signals, execution timeline, BTC position, trade summary, runtime health)
- `/mobile` — mobile-first bot status grid, generated by `buildMobileBotStatus.js`
- `/api/mobile-bot-status` — JSON endpoint backing the mobile view

**How data is sourced:**
- **Bot registry**: `fs.readFileSync` on `/home/ubuntu/.openclaw/workspace/Q_S2/config/bots.json` — read fresh on every request, no caching.
- **DB**: hardcoded fallback `/tmp/qs2_review/data/s2.sqlite` in dashboard code — but the webhook process (`q-s2-webhook.service`) writes to `/home/ubuntu/.openclaw/workspace/Q_S2/data/s2.sqlite` (confirmed via `/proc/<pid>/environ`). The dashboard reads a stale rsync copy; both are in sync immediately post-deploy but diverge as the webhook appends new records. This mismatch must be resolved in Phase 4.
- **Bybit positions**: live API calls per bot using per-bot credentials from bots.json.
- **Heartbeat**: reads from `heartbeat_events` table via `buildPersistence`.

**What's broken / stale:**

| Issue | Root cause |
|-------|-----------|
| Bot1 shows STXUSDT, Bot4 CRVUSDT, Bot7 FLOKIUSDT | Deploy of commit 48a258d (symbol changes) has not propagated to EC2 workspace yet at time of screenshot capture. `bots.json` is read fresh per request so will self-correct once the `git pull` in `deploy.sh` completes. |
| `q-s2-dashboard` not restarted by `deploy.sh` | `deploy.sh` only restarts `q-s2-webhook`. If dashboard JS source changes, the old process continues serving old code until manually restarted. Data files (bots.json, DB) update without restart; code changes do not. |
| Bot card missing MDX-resolved data | Mobile bot card shows symbol + enabled + trade state + balance + uPnL. It does NOT show MDX profile name or leverage. These come from `resolveBotSettings` but aren't called in `buildMobileBotStatus`. |
| Heartbeat "n/a — status unknown" | `heartbeat_events` table is either empty or the heartbeat write path is not yet implemented in the webhook flow. The `loadHeartbeatStatus` function handles this gracefully but returns nulls. |
| Full dashboard shows BTCUSDT position | `loadCurrentPositionAndOrders` hardcodes `BTCUSDT` and uses `BYBIT_TESTNET_API_KEY` — a single legacy key, not per-bot routing. Unrelated to the multi-bot architecture. |

### 1.3 S4 Dashboard (ports 80 and 8080)

Static HTML files served by `python -m http.server`. No backend. Data loaded from JSON files written by the Python services:

- **S4 classic** (`/home/ubuntu/s4/s4_dashboard.html`): Pump Detector parameter sweep across 32 instances. Shows: total signals detected, coins being watched, active trades count, win rate, total PnL, best-performing instance, recent pumps table (last 12h), and per-instance performance grid. Reads `s4_dashboard_data.json`.
- **S4_2** (`/home/ubuntu/s4_2/s4_2_dashboard.html`): Same structure, separate sweep.
- **S4 EMA Live** (`/home/ubuntu/s4_ema_live/`): No dashboard. State in `state.json` (trade_num, total_pnl, saved_at) and `trades.json`.

**Issues:** No auth. Direct port exposure (port 80 is world-accessible). No live S4 EMA data visible in any dashboard.

### 1.4 S6 Signal Scout Dashboard (port 8082)

Flask app at `/home/ubuntu/S6/`. Routes:
- `/` — signal review queue (ranked candidates with scores)
- `/active` — active paper trades
- `/history` — closed trade history
- `/trades` — trade log page
- `/desktop` — desktop layout
- `/target-trades` — target setup list

API endpoints: `/api/queue`, `/api/active`, `/api/history`, `/api/btc`, `/api/chart/<symbol>`, `/api/minichart/<symbol>`, `/api/trades`, `/api/trades/open`, `/api/trade/<id>/close`, `/api/account`, `/api/analyse` (Claude Vision proxy), `/s6/state`, `/s6/toggle`, `/s6/enable`, `/s6/disable`.

Features: chart view (lightweight-charts), MTF strip, BTC context panel, Claude Vision AI analysis (Anthropic key server-side), S6 enable/disable toggle.

**Issues:** Not accessible via dashboard.tbotsys.one — direct IP:port only. No auth.

### 1.5 S6 Funnel Dashboard (port 8083)

Flask app at `/home/ubuntu/S6_FUNNEL/`. Routes include `/intraday`, `/northstar/<symbol>` (North Star setup library). Shows: queued signals with scores, active paper trades with entry/TP/SL, BTC context. Trade monitor runs in background thread. Template-rendered via Jinja2.

**Issues:** Direct IP:port only. Not tunnelled. No auth.

---

## 2. Proposed Unified Portal Architecture

### 2.1 Overview

A new Node.js Express app (`portal`) replaces the current S2 dashboard as the process on port 3010, tunnelled through the existing `dashboard.tbotsys.one` cloudflare route. No tunnel changes required. The portal serves:

- `/` — unified landing page (health summary of all three systems)
- `/s2/*` — S2 section (bot status, signals, P&L)
- `/s4/*` — S4 section (pump detector stats, EMA live trades)
- `/s6/*` — S6 section (signal queue, paper trades, charts)
- `/api/*` — data API endpoints consumed by the frontend
- `/mobile` — preserved for backwards compatibility, now shows all-systems summary

### 2.2 Navigation

Top-level tab bar, mobile-first:

```
[ S2 — 8 bots ] [ S4 — Pump Detector ] [ S6 — Signal Scout ]
```

Each tab is a separate page/view. The landing page (`/`) shows a compact strip per system:
- S2: N bots live, total account value, last signal
- S4: N signals today, best instance PnL
- S6: queue depth, N active trades, total unrealised PnL

### 2.3 Authentication

Simple token-based access via a shared secret in a cookie. On first visit, redirect to `/login`. Login page accepts a password (env var `PORTAL_PASSWORD`). Sets an `HttpOnly` session cookie (HMAC-signed token). All routes check the cookie. No external auth service needed for single-operator use.

This is sufficient for a personal dashboard — the threat model is accidental exposure, not targeted attacks.

### 2.4 Data architecture

| System | Data source | Access method |
|--------|------------|--------------|
| S2 bots | `config/bots.json` + `resolveBotSettings` + Bybit API | Direct (same process) |
| S2 DB | `/home/ubuntu/.openclaw/workspace/Q_S2/data/s2.sqlite` | Direct SQLite read |
| S4 classic | `/home/ubuntu/s4/s4_dashboard_data.json` | `fs.readFile` |
| S4_2 | `/home/ubuntu/s4_2/s4_2_dashboard_data.json` | `fs.readFile` |
| S4 EMA | `/home/ubuntu/s4_ema_live/state.json`, `trades.json` | `fs.readFile` |
| S6 Signal Scout | `localhost:8082/api/*` | HTTP proxy |
| S6 Funnel | `localhost:8083/api/*` | HTTP proxy |

This avoids touching S4 and S6 internals — the portal is a read (and thin-proxy) layer only.

### 2.5 What happens to existing services

- S6 at port 8082 and 8083: remain running unchanged. The portal proxies their APIs. Direct IP:port access stays available for development.
- S4 at ports 80/8080: remain running. Portal reads their JSON data files directly.
- S2 webhook at port 3001: unaffected.
- `q-s2-dashboard.service`: replaced by `q-portal.service` which runs the new portal. Port 3010 stays the same.

---

## 3. S2 Section Requirements

| Item | Source | How to get it |
|------|--------|--------------|
| Live bot registry | `config/bots.json` + `resolveBotSettings` | Read fresh per request; already done in `buildMobileBotStatus`, needs MDX layer added |
| Per-bot symbol, profile, leverage, enabled | `resolveBotSettings(botId)` | `ctx.symbol`, `ctx.mdx.profile`, `ctx.settings.positionSizing.leverage`, `ctx.bot.enabled` |
| Per-bot wallet balance | Bybit `/v5/account/wallet-balance` per bot | Already in `buildMobileBotStatus.fetchBotStatus` |
| Per-bot open position | Bybit `/v5/position/list?symbol=X` per bot | Already in `buildMobileBotStatus.fetchBotStatus` |
| Per-bot realised P&L (today, 7d, all-time) | SQLite `exit_events` filtered by bot_id + date | New query needed |
| Last signal per bot | SQLite `normalized_signals` filtered by bot_id | New query needed |
| Last order attempt per bot | SQLite `order_attempts` filtered by bot_id | New query needed |
| Service heartbeat | `heartbeat_events` table | Already in `loadHeartbeatStatus` — needs write side implemented |
| Management loop status | Log file or DB event | Not currently written to DB; could be added as `management_loop_events` |
| Quick actions (pause/resume bot) | Modify `bots.json` enabled state, restart webhook | Requires write API endpoint — out of scope for read-only Phase 4 dashboard |

---

## 4. S4 Section Requirements

Based on current S4 classic dashboard:

| Item | Available? |
|------|-----------|
| Total signals detected | Yes — in `s4_dashboard_data.json` |
| Coins being watched | Yes |
| Active trades | Yes |
| Win rate, total PnL | Yes |
| Best-performing instance | Yes |
| Recent pumps table | Yes |
| Per-instance performance grid | Yes |
| S4 EMA Live state (trade_num, total_pnl) | Yes — `state.json` |
| S4 EMA Live open trades | Yes — `trades.json` |
| S4 EMA Live trade history | Yes — `trades.json` |
| S4_2 pump data | Yes — `s4_2_dashboard_data.json` |

Missing from any current dashboard: S4 EMA Live has no web view. The portal should add a simple trades table for it.

---

## 5. S6 Section Requirements

Based on existing S6 Signal Scout (port 8082) and S6 Funnel (port 8083):

**S6 Signal Scout panel:**
- Signal review queue (ranked candidates) — proxy `/api/queue`
- BTC context — proxy `/api/btc`
- Active paper trades — proxy `/api/trades/open`
- Closed trade history — proxy `/api/history`
- Chart view per symbol — proxy `/api/chart/<symbol>`
- S6 enable/disable toggle — proxy `/s6/toggle`
- Claude Vision AI analysis — proxy `/api/analyse`

**S6 Funnel panel:**
- Open paper trades with entry/TP/SL — proxy `/api/trades/open` (Funnel)
- North Star setups per symbol — proxy Funnel northstar state
- Queue candidates — proxy Funnel `/api/queue`

**Currently missing / should add:**
- Unified S6 unrealised P&L total across both Signal Scout and Funnel trades
- Last scan time
- Watchlist size

---

## 6. Technical Approach

### Stack

New portal: **Node.js + Express**. No frontend build step — server-rendered HTML with inline CSS and lightweight vanilla JS for polling. Same pattern as the current S2 dashboard. No React, no bundler, no CDN dependencies.

Why Node over Python: S2 is already Node, `resolveBotSettings` and `botRegistry` are Node modules, and the portal integrates most tightly with S2. Avoids a polyglot process boundary for the primary data source.

### Routing at dashboard.tbotsys.one

No change needed. The cloudflare tunnel already routes `dashboard.tbotsys.one → localhost:3010`. The portal runs on 3010 and replaces the current S2 dashboard process.

### S6 data access

Portal makes HTTP GET requests to `localhost:8082/api/*` (S6 Scout) and `localhost:8083/api/*` (S6 Funnel) from within the Express server. These are internal loopback calls — no auth headers needed since both services bind to all interfaces locally.

### S4 data access

Portal reads JSON files directly from disk using `fs.readFile`. The S4 services write these files every few seconds. Stale tolerance is acceptable given S4 runs on a sweep cadence (not sub-second).

### Polling / refresh

Mobile page: `<meta http-equiv="refresh" content="15">` — same as current S2. No WebSocket required.

### Deployment

New systemd unit `q-portal.service` replaces `q-s2-dashboard.service`. `deploy.sh` updated to restart `q-portal` on deploy. Current `q-s2-dashboard.service` unit can be kept but stopped.

---

## 7. Implementation Plan

### Phase 2 (immediate, this session): Fix S2 stale bot registry

**Problem:** `deploy.sh` does not restart `q-s2-dashboard`. If dashboard JS code changes, old code keeps running. Separately, bot cards do not show MDX-resolved profile or leverage.

**Fix:**
1. Add `sudo systemctl restart q-s2-dashboard` to `deploy.sh` after the webhook restart.
2. Update `buildMobileBotStatus.js`: for each bot, call `resolveBotSettings(botId)` to get `mdxProfile` and `leverage`, add them to the returned bot object.
3. Update bot card template in `createDashboardServer.js` to display profile and leverage below the symbol line.

**Verification:** Reload `dashboard.tbotsys.one/mobile` — Bot1 shows DEEPUSDT, Bot4 shows ZECUSDT + Enabled, Bot7 shows 1000FLOKIUSDT. Bot cards show "Balanced · 3x" etc.

**Estimate:** ~1 hour.

### Phase 3: Portal framework (navigation shell + landing page)

New Express app at port 3010. Implements:
- Auth middleware (cookie + password)
- Top-level nav (S2 / S4 / S6 tabs)
- Landing page with 3-system health strip
- Placeholder pages for S4 and S6 sections
- Mobile-first CSS

S2 existing code migrated as a module into the portal. S4 and S6 sections serve "coming soon" panels.

**Estimate:** 1–2 days.

### Phase 4: Full S2 section

Migrate and extend current S2 dashboard into the portal:
- Per-bot card with MDX profile, leverage, balance, open position, realised P&L (today/7d)
- Fix DB path: portal reads from `/home/ubuntu/.openclaw/workspace/Q_S2/data/s2.sqlite` (live webhook DB, not stale /tmp copy)
- Heartbeat implement write side: webhook pings `heartbeat_events` on each processed signal
- Management loop status: write `management_loop_events` on each loop fire
- Signal feed per bot (filter `normalized_signals` by bot_id)
- Service health panel (webhook active, tunnel active, config valid)

**MDX renewal panel** (prominent, top of S2 section):

The S2 page opens with a renewal countdown so the operator immediately knows whether MDX needs renewing without hunting for context.

- `MDX_RENEWAL_DATE` env var (format `YYYY-MM-DD`) added to `/home/ubuntu/.openclaw/.env`. Document in runbook alongside other S2 env vars.
- Large days-remaining display, colour-coded: green (>30d) / amber (8–30d) / red (≤7d).
- Review criteria from `docs/review-triggers-2026-05-08.md` rendered directly beneath the countdown — no context switching needed.
- Live DB-calculated status against each criterion:

| Criterion | DB query | Pass threshold |
|-----------|----------|----------------|
| Portfolio P&L positive | `SUM(realised_pnl)` from `exit_events` for current MDX period | > 0 |
| Per-bot max drawdown | Max consecutive loss run per `bot_id` in `exit_events` | ≤ 25% account |
| Win rate | `COUNT(WHERE realised_pnl > 0) / COUNT(*)` per bot | ≥ 40% |
| Trade count | `COUNT(*)` from `exit_events` since period start | ≥ 30 |

Each criterion renders as green tick / amber warning / red cross with the live value beside it. Goal: operator opens dashboard → single glance → renew or not.

**Estimate:** 2–3 days.

### Phase 4.5: Native Bybit SL at entry

**Prerequisite before scaling to 65% margin sizing.** Must be completed before the 2026-05-08 review.

S2 currently manages stop-loss entirely via internal polling (15s loop). During the 2-3 day crash (2026-04-22 to 2026-04-24), positions were naked — no exchange-side protection. This phase adds a belt-and-braces native Bybit stop order placed simultaneously with every market entry.

**Implementation:**
- In `submitEntryOrder` (`bybitExecution.js`), immediately after the entry market order fills, place a second order: `POST /v5/order/create` with `orderType: "Market"`, `triggerPrice: <sl_price>`, `triggerBy: "LastPrice"`, `reduceOnly: true`, `closeOnTrigger: true`, opposite side.
- SL price derived from `settings.stopLoss.triggerPercent` and `entryPrice` — already available in the execution context.
- Log the native SL order ID to `exit_events` with `exit_reason: "native_sl_placed"` so the dashboard can confirm it was set.
- If native SL placement fails (e.g. insufficient margin for conditional order), log the error but do NOT block the entry — internal polling SL remains the fallback.

**This does not replace the internal polling SL** — both run in parallel. If the position is closed by TP before the native SL triggers, the native SL order should be cancelled; add cancel logic to the TP close path.

**Estimate:** 4–8 hours.

### Phase 5: S4 and S6 sections

S4 section: read and render `s4_dashboard_data.json`, `s4_2_dashboard_data.json`, `s4_ema_live/state.json` + `trades.json`. No changes to S4 internals.

S6 section: proxy calls to localhost:8082 and localhost:8083, render combined view of Signal Scout queue + Funnel open trades + BTC context.

**Estimate:** 2 days.

### Phase 6: Signal intelligence layer (CC async analysis)

When S2 receives an entry signal and places a trade, a non-blocking analysis job is queued. The trade executes first; analysis follows asynchronously.

**Flow:**

1. After a successful entry order, S2 writes a job file to `/home/ubuntu/s2/data/analysis_queue/` containing: `botId`, `symbol`, `trade_id`, `signal_direction`, `timestamp`, last 100 candles of 1H OHLCV, last 100 candles of 4H OHLCV (fetched from Bybit public API at signal time, already available via `getLiveReferencePrice` infrastructure).
2. A CC worker (persistent Claude Code session on EC2) polls the queue directory. On each job: renders a candlestick chart image using `mplfinance` (headless, no display required), runs S6-style vision analysis on the chart image, writes result back to S2 DB.
3. Dashboard surfaces the result per trade and in aggregate stats.

**DB schema additions** (new table `s6_analysis`):
```sql
CREATE TABLE s6_analysis (
  id INTEGER PRIMARY KEY,
  trade_id TEXT NOT NULL,
  bot_id TEXT NOT NULL,
  symbol TEXT NOT NULL,
  signal_direction TEXT NOT NULL,
  s6_directive TEXT NOT NULL,       -- TRADE / WAIT / AVOID / MONITOR
  s6_conviction_score REAL,         -- 0.0–1.0
  s6_analysis_text TEXT,
  chart_image_path TEXT,
  analysis_timestamp TEXT NOT NULL,
  model_used TEXT
);
```

**Queue watching mechanism:**

Persistent Claude Code session on EC2 using `/loop` mode with a 60s poll interval. On each tick: `ls /home/ubuntu/s2/data/analysis_queue/*.json 2>/dev/null`, process any files found, move processed files to `analysis_queue/done/`. Restart-safe — unprocessed files persist across CC restarts. Alternative: systemd timer running `claude --print "process queue" every 60s` — simpler but loses conversational context between runs. Recommend persistent session for richer prompt continuity.

**Chart rendering:** `mplfinance` confirmed. Headless — no DISPLAY env var needed on EC2 (uses `matplotlib` Agg backend). Install: `pip install mplfinance` in a venv alongside the CC worker. Renders to PNG at `/home/ubuntu/s2/data/charts/<trade_id>.png`. Chart shows: 1H candlesticks (100 bars), volume, 20/50 EMA overlay, entry price line, SL and TP1/TP2 levels marked.

**S6 prompt variant for MDX trend-following:** The S6 Signal Scout is tuned for mean-reversion setups (score-based queue). MDX signals are trend-following (momentum breakout with ATR-based SL). The analysis prompt must be adapted: focus on trend continuation confirmation (HTF alignment, momentum, volume), not mean-reversion exhaustion signals. Prompt stored in a config file so it can be refined without code changes.

**Per-bot filter mode** (`s6FilterMode` in `bots.json`):

| Mode | Behaviour |
|------|-----------|
| `log` | Always execute. Record directive and outcome. Default for all bots. |
| `soft` | Skip AVOID signals — all others execute. |
| `hard` | Execute TRADE only — skip WAIT, AVOID, MONITOR. |

All bots start on `log`. Move to `soft` only after 50+ closed trades show a statistically clear win-rate differential between TRADE and AVOID categories. This threshold and process is documented in `docs/review-triggers-2026-05-08.md`.

**Dashboard surfaces:**
- Per-trade: MDX signal direction, S6 directive at signal time, conviction score, chart thumbnail, trade outcome (when closed)
- Aggregate stats view: win rate and avg P&L broken down by directive category (TRADE / WAIT / AVOID / MONITOR)
- Per-bot filter mode badge on bot card

**Estimate:** 3–4 days (CC worker + mplfinance setup + DB schema + dashboard display).

### Phase 7: Auth layer

Add password-protected login at `/login`. Shared `PORTAL_PASSWORD` env var. HMAC-signed session cookie. Block all routes without valid cookie.

**Estimate:** 4 hours.

---

## 8. Phase estimates summary

| Phase | Description | Estimate |
|-------|-------------|----------|
| 2 | Fix S2 stale bot registry + deploy restart | ~1 hour ✓ done |
| 3 | Portal framework: nav shell, landing, auth | 1–2 days |
| 4 | Full S2 section + MDX renewal panel | 2–3 days |
| 4.5 | Native Bybit SL at entry (pre-2026-05-08) | 4–8 hours |
| 5 | S4 + S6 sections | 2 days |
| 6 | Signal intelligence layer (CC async analysis) | 3–4 days |
| 7 | Auth layer | 4 hours |
| Total | | ~10–13 days |

---

## 9. Constraints respected

- `dashboard.tbotsys.one` domain unchanged — same cloudflare tunnel, same port 3010
- Mobile-first — all views designed for 375px+ viewport first
- S2 webhook and management loop unaffected throughout all phases
- S4 and S6 internals untouched — portal is read-only layer
- No CDN dependencies — all assets served locally or inline
